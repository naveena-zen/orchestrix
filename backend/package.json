{
  "name": "orchestrix-backend",
  "version": "1.0.0",
  "description": "Workflow Automation System Backend",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "nodemon src/index.js",
    "migrate": "npx prisma migrate deploy",
    "generate": "npx prisma generate",
    "seed": "node src/seed.js",
    "setup": "npx prisma migrate deploy && npx prisma generate && node src/seed.js"
  },
  "dependencies": {
    "@prisma/client": "^5.10.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.3",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.0",
    "prisma": "^5.10.2"
  }
}
